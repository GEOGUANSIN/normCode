# NormCode Compiler - Draft Plan
# This is the compiler written as a NormCode plan
# It uses ChatTool and CanvasTool to interact with users

<- compilation result
    <= report: send success message to chat and load graph on canvas
    <- activated plan
        <= activate: generate concept.json and inference.json from structure
        <- confirmed formalization
            <= confirm formalization: ask user to approve via chat
            <- formalized plan
                <= formalize: add types, operators, paradigms using LLM
                <- confirmed derivation
                    <= confirm derivation: ask user to approve derived concepts via chat
                    <- derived structure
                        <= derive: parse input into concepts and inferences using LLM
                        <- user input
                            <= read input: get draft from user via chat
                            <- chat initialized
                                <= initialize: display welcome message in chat

