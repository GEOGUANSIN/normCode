{
  "name": "chat_ask",
  "description": "Ask user a question via chat with optional choices",
  "vertical_inputs": {
    "question": "The question to ask"
  },
  "horizontal_inputs": {
    "options": "Optional list of choices to display as buttons"
  },
  "sequence": ["python_execute"],
  "execution": {
    "type": "python",
    "script": "opts = horizontal.get('options')\nresult = Body.chat.ask(question, options=opts)"
  },
  "output_type": "string"
}

