"""
Constant path definitions for the NormCode agent translation system
"""

import os
from pathlib import Path

# Current directory (agent_translation)
CURRENT_DIR = Path(__file__).parent.absolute()

# Project root directory (NormCodeCompile)
PROJECT_ROOT = CURRENT_DIR.parent.absolute() 