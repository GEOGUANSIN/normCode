{
  "paradigm_id": "h_Data-c_ExtractField-o_Literal",
  "description": "Extract a specific field from a dictionary or structured data",
  "body_faculty": "python_interpreter",
  
  "inputs": {
    "horizontal": {
      "data": {
        "norm": "in-memory",
        "description": "Dictionary or structured data",
        "required": true
      },
      "field_path": {
        "norm": "in-memory",
        "description": "Path to field (e.g., 'operations' or 'nested.field')",
        "required": false,
        "default": "inferred from caller context"
      }
    }
  },
  
  "composition": {
    "script": "scripts/extract_field.py",
    "function": "extract_field",
    "steps": [
      {
        "step": "parse_path",
        "action": "split field_path by '.'",
        "output": "path_parts"
      },
      {
        "step": "traverse",
        "action": "navigate data using path_parts",
        "output": "extracted_value"
      }
    ]
  },
  
  "output": {
    "format": "o_Literal",
    "type": "any",
    "description": "The extracted field value"
  },
  
  "notes": [
    "Field path is inferred from functional concept",
    "e.g., 'get operations from extraction data' → field_path = 'operations'"
  ]
}

