{
  "paradigm_id": "h_FilePath-h_Content-c_AppendFile-o_Literal",
  "description": "Append string content to file",
  "body_faculty": "file_system",
  
  "inputs": {
    "horizontal": {
      "file_path": {
        "norm": "in-memory",
        "description": "Path to the file to append to",
        "required": true,
        "order": 1
      },
      "content": {
        "norm": "in-memory",
        "description": "String content to append",
        "required": true,
        "order": 2
      }
    }
  },
  
  "composition": {
    "steps": [
      {
        "step": "ensure_dir",
        "action": "create parent directories if needed",
        "output": null
      },
      {
        "step": "append",
        "action": "body.file_system.append(h.file_path, h.content + '\\n')",
        "output": "append_result"
      }
    ]
  },
  
  "output": {
    "format": "o_Literal",
    "type": "str",
    "description": "Confirmation string (e.g., 'appended to path/to/file')"
  },
  
  "error_handling": {
    "permission_error": "propagate error",
    "disk_full": "propagate error"
  }
}

